# Stadia Maps API key (required for map tiles).
# Get one at https://client.stadiamaps.com/
VITE_STADIA_API_KEY=

# Optional: base path for production (e.g. GitHub Pages subpath). Default is /.
# VITE_BASE_PATH=/
